{
  "name": "remote_codex",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts ./src/server.ts ./src/utils.ts ./src/streaming.ts ./src/session.ts ./src/approval.ts ./src/slash.ts ./src/queue.ts ./src/runtime.ts ./src/appServerRuntime.ts ./src/claudeCodeRuntime.ts ./src/workdir.ts --target=node --outdir=dist --entry-naming='[name].[ext]' --external=@larksuiteoapi/node-sdk --external=@openai/codex-sdk --external=express --external=cors --external=dotenv",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "keywords": [
    "codex",
    "feishu",
    "lark",
    "bot",
    "mcp"
  ],
  "author": "Antigravity",
  "license": "MIT",
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.58.0",
    "@openai/codex-sdk": "^0.95.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
